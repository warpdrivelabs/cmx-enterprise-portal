# 生产构建专用环境变量（vite build 时 mode=production 自动加载）
#
# 同源托管到后端 /html/ 路径下（cmx-container dev.toml [portal] web_html_dist 配置）
# 访问：http://<host>:8080/html/
#
# 部署到其他路径只需改本文件，如 nginx 反代到 /cmx-designer/：
#   CMX_APP_BASE=/cmx-designer/

CMX_APP_BASE=/html/

# 后端 API 基础地址（留空 = 同源，前后端同站托管时无需配置）。
# 若前后端不同源部署（前端在 A 域，后端在 B 域，无法用 nginx 反代），设为后端地址：
#   VITE_API_BASE=https://api.example.com
VITE_API_BASE=
